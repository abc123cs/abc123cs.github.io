CREATE TRIGGER t_update_goods BEFORE UPDATE ON goods
       FOR EACH ROW 
       BEGIN 
           IF New.goods_price >100 THEN 
               SET New.goods_name = CONCAT('gaoduan',New.goods_name); 
           END IF; 
       END;
